stages:
  - build
  - test
  - deploy

before_script:
  - ls -l
  - ruby -v
  - which ruby

generate_asciidoc:
  stage: build
  tags:
    - ruby
  script:
    - gem install asciidoctor
    - ./generate_adoc_html.sh
  artifacts:
    paths:
      - Oracle_performance_analysis_with_Panorama_from_adoc.html